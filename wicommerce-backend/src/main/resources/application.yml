spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wicommerce_db
    username: wicomm_user
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true